# App
NODE_ENV=production
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-me-to-a-secure-random-string
JWT_SECRET=change-this-too

# MongoDB connection string (from app container to mongodb service)
# MONGODB_URI=mongodb://root:secret@mongodb:27017/tours_republica?authSource=admin

# Email (use safe app password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=nureazam97@gmail.com
SMTP_PASS=YOUR_SMTP_APP_PASSWORD

# Optional: email (for production) - if absent, reset link will be printed to server console
NEXT_PUBLIC_APP_URL=http://localhost:3000/
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=nureazam97@gmail.com
EMAIL_PASS=deju qfdd qijb xyae

